# Gotchas

Add only verified traps that are likely to recur. Record the observed behavior, workaround, and proof. Remove or mark entries superseded once the underlying condition changes.

| Topic | Use when |
| --- | --- |
| `app-template-dependency-pins.md` | changing versions or install settings in `.harness/app-template` |
