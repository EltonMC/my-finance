# Copy to .env.local and fill with the values printed by `pnpm db:start`.
# Only public values belong here: the browser receives every VITE_ variable.
# Never add service_role or sb_secret_ keys to this file.
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_PUBLISHABLE_KEY=
